<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.FragmentFecha">

    <data>
        <variable
            name="viewModel"
            type="com.intermodular.guardiapp.viewmodel.AvisoGuardiaViewModel" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="25dp">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <DatePicker
                android:id="@+id/datePicker"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:datePickerMode="calendar"
                android:calendarTextColor="@color/moradoPaleta"
                android:dayOfWeekBackground="@color/moradoPaleta"
                android:dayOfWeekTextAppearance="@color/moradoPaleta"
                android:defaultFocusHighlightEnabled="true"
                android:headerBackground="?attr/actionModeSplitBackground" />

            <Button
                android:id="@+id/botonSiguiente"
                android:layout_width="150dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:background="@drawable/rounded_corner"
                android:text="Siguiente" />
        </LinearLayout>

    </FrameLayout>
</layout>